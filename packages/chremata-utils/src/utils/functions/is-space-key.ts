/**
 * Checks if the given key is a space character.
 *
 * @param key - The key to check.
 *
 * @returns `true` if the key is a space character, `false` otherwise.
 */
export function isSpaceKey(key: string): boolean {
  return key === ' ';
}
