/**
 * Checks if the provided key is the "Enter" key.
 *
 * @param key - The key to check.
 *
 * @returns `true` if the key is "Enter", `false` otherwise.
 */
export function isEnterKey(key: string): boolean {
  return key === 'Enter';
}
